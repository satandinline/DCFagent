fastapi==0.115.0
uvicorn[standard]==0.30.6
openai>=1.58.0,<2.0.0
httpx>=0.27.0,<1.0.0
pdfplumber==0.11.4
pymupdf>=1.24.0  # Fast PDF parsing (optional but recommended)
python-multipart==0.0.12
pydantic>=2.9.0,<3.0.0
python-dotenv==1.0.1
pymysql>=1.1.0
sqlalchemy>=2.0.0
yfinance>=0.2.54  # FREE financial data API (no API key needed)
ddgs>=8.0.0  # FREE DuckDuckGo search (no API key needed, renamed from duckduckgo-search)
googlesearch-python>=1.3.0  # FREE Google scraping (fallback, no API key needed)
APScheduler>=3.10.0  # For scheduling periodic agent tasks

# LangChain and LangGraph for AI workflow
langchain>=0.3.0
langgraph>=0.2.0
langchain-core>=0.3.0

# File parsing libraries
openpyxl>=3.1.0  # Excel (.xlsx, .xls) parsing
python-docx>=1.1.0  # Word (.docx) parsing

# Vector database for semantic memory (optional)
chromadb>=0.4.0  # Vector storage for agent memory
