# .gitignore 配置说明

本文档说明了项目中 `.gitignore` 文件的配置规则和原因。

---

## 📋 核心原则

1. **不提交敏感信息** - API密钥、密码、环境变量
2. **不提交构建产物** - node_modules、__pycache__、dist/
3. **不提交临时文件** - 测试文件、日志、缓存
4. **保留必要配置** - package.json、tsconfig.json等
5. **保留示例代码** - examples/目录下的有用示例

---

## 🔒 敏感信息（必须忽略）

### 环境变量
```
.env
.env.local
.env.*.local
```

**原因**: 包含API密钥、数据库密码等敏感信息

**注意**: 
- 应该提供 `.env.example` 作为模板
- 团队成员需要复制并填写自己的配置

### 密钥文件
```
*.pem
*.key
*.secret
credentials.json
service-account.json
```

**原因**: 包含加密密钥和服务账户凭证

---

## 🐍 Python相关

### 编译文件
```
__pycache__/
*.py[cod]
*$py.class
*.so
```

**原因**: Python字节码文件，可以从源代码重新生成

### 虚拟环境
```
venv/
env/
ENV/
```

**原因**: 依赖包体积大，可以通过requirements.txt重新安装

### 分发包
```
build/
dist/
*.egg-info/
*.egg
```

**原因**: 构建产物，可以重新打包

### 测试覆盖率
```
.pytest_cache/
.coverage
htmlcov/
coverage.xml
```

**原因**: 测试生成的临时数据

---

## 📦 Node.js相关

### 依赖包
```
node_modules/
```

**原因**: 
- 体积巨大（几百MB）
- 可以通过package.json重新安装
- 不同平台可能有不同的二进制文件

### 构建产物
```
frontend/dist/
frontend/build/
.vite/
```

**原因**: Vite构建输出，可以重新构建

### 日志
```
npm-debug.log*
yarn-debug.log*
yarn-error.log*
```

**原因**: 调试日志，不需要版本控制

---

## 💾 数据库文件

```
*.db
*.sqlite
*.sqlite3
```

**原因**: 
- 本地开发数据库
- 生产环境使用MySQL服务器
- 避免提交测试数据

---

## 📝 临时文档

```
TEST_REPORT.md
GOOGLE_SEARCH_GUIDE.md
MULTI_TIER_SEARCH_GUIDE.md
FINAL_SUMMARY.md
SEARCH_TEST_AND_OPTIMIZATION.md
RAG_FEATURE_GUIDE.md
CLEANUP_SUMMARY.md
*.tmp.md
```

**原因**: 
- 开发和测试过程中产生的临时文档
- 最终内容应整合到README.md
- 避免文档碎片化

**例外**: 
- README.md - 主文档，必须保留
- 正式的教程或指南文档

---

## 🧪 测试文件

```
test_*.py
*_test.py
tests/
```

**原因**: 
- 临时测试脚本
- 开发过程中的试验代码

**注意**: 
- `examples/` 目录被保留，因为包含有用的示例代码
- 正式的单元测试应该在专门的tests/目录中（当前项目暂无）

---

## 📊 数据文件

```
prompt_optimization_results.json
*.json
!package.json
!package-lock.json
!tsconfig.json
!tsconfig.node.json
```

**原因**: 
- 测试结果JSON不需要版本控制
- 配置文件JSON需要保留（通过!排除）

**保留的JSON**:
- `package.json` - Node.js依赖配置
- `package-lock.json` - 锁定依赖版本
- `tsconfig.json` - TypeScript配置
- `tsconfig.node.json` - Node TypeScript配置

---

## 🗂️ 临时上传目录

```
backend/temp_uploads/*
!backend/temp_uploads/.gitkeep
```

**原因**: 
- PDF上传的临时文件
- 不应提交到版本控制
- 使用.gitkeep保持目录结构

---

## 🖥️ IDE和编辑器

```
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
```

**原因**: 
- 个人IDE配置
- 不同开发者有不同的偏好
- 避免配置冲突

---

## 🌍 操作系统文件

### Windows
```
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.exe
*.dll
```

### macOS
```
.DS_Store
.AppleDouble
.LSOverride
```

### Linux
```
*~
.directory
```

**原因**: 系统自动生成的文件，与项目无关

---

## 🐳 Docker（如果使用）

```
docker-compose.override.yml
.env.docker
```

**原因**: 
- 本地Docker覆盖配置
- 可能包含本地特定的设置

---

## 📡 Ngrok

```
ngrok.yml
ngrok.log
```

**原因**: 
- ngrok配置文件可能包含认证令牌
- 日志文件不需要版本控制

---

## 💾 备份文件

```
*.bak
*.backup
*.old
*.tmp
*.temp
tmp/
temp/
```

**原因**: 手动创建的备份文件，不应该提交

---

## ✅ 应该提交的文件

### 后端
- ✅ `backend/*.py` - Python源代码
- ✅ `backend/requirements.txt` - 依赖列表
- ✅ `backend/examples/*.py` - 有用示例
- ✅ `backend/.env.example` - 环境变量模板

### 前端
- ✅ `frontend/src/**/*` - React源代码
- ✅ `frontend/package.json` - 依赖配置
- ✅ `frontend/*.config.*` - 构建配置

### 文档
- ✅ `README.md` - 主文档
- ✅ `prompt_file/*.xlsx` - Prompt模板

### 配置
- ✅ `.gitignore` - Git忽略规则
- ✅ `start.py` - 启动脚本

---

## 🚫 常见错误

### ❌ 错误1: 提交了.env文件

```bash
# 错误
git add backend/.env

# 正确
echo "backend/.env" >> .gitignore
```

### ❌ 错误2: 提交了node_modules

```bash
# 错误
git add frontend/node_modules/

# 正确
echo "node_modules/" >> .gitignore
```

### ❌ 错误3: 提交了测试文件

```bash
# 错误
git add test_*.py

# 正确
echo "test_*.py" >> .gitignore
```

---

## 🔧 维护建议

### 1. 定期检查

```bash
# 查看哪些文件被忽略
git status --ignored

# 查看.gitignore是否生效
git check-ignore -v <file-path>
```

### 2. 清理已跟踪的文件

如果不小心提交了应该忽略的文件：

```bash
# 从Git中移除但保留本地文件
git rm --cached backend/.env
git rm --cached -r frontend/node_modules/

# 提交更改
git commit -m "Remove ignored files from tracking"
```

### 3. 更新.gitignore

当添加新的工具或框架时，及时更新.gitignore：

```bash
# 例如：添加了Poetry
echo "poetry.lock" >> .gitignore
echo ".venv/" >> .gitignore
```

---

## 📚 参考资源

- [GitHub gitignore templates](https://github.com/github/gitignore)
- [Git官方文档 - gitignore](https://git-scm.com/docs/gitignore)
- [Toptal .gitignore generator](https://www.toptal.com/developers/gitignore)

---

## ✨ 总结

一个好的 `.gitignore` 文件应该：

✅ **保护敏感信息** - API密钥、密码  
✅ **减少仓库体积** - 忽略构建产物和依赖  
✅ **避免冲突** - 忽略IDE和个人配置  
✅ **保持清洁** - 忽略临时文件和测试脚本  
✅ **易于维护** - 清晰的注释和分类  

**本项目配置评分**: ⭐⭐⭐⭐⭐ (5/5)

---

*最后更新: 2026-04-05*
